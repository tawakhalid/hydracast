node_modules/
out/
release/
dist/
# esbuild bundles the test harnesses to these before running them; one pattern
# rather than a line per suite, which is how the list got out of date.
test/.*.cjs
*.tsbuildinfo
*.log
.env
.env.*
!.env.example
.DS_Store

# Screenshots dropped in the repo root while debugging
/*.png
